---
title: "PA1_template"
author: "Paula Sichel"
date: "December 16, 2015"
output: html_document
---

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r, echo=TRUE}
# Access the zip file from the course website, unzip as .csv file
temp <- tempfile()
download.file("http://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip", 
              temp)
temp <- unz(temp, "activity.csv")
activity <- read.csv(temp)
unlink(temp)
```

```{r, echo=TRUE}
# Create a historgram of the activity dataset
activity$date <- as.Date(activity$date)
days <- aggregate(steps ~ date, data=activity, FUN="sum")

hist(days$steps)


#Calculate the median and mean 
median(activity$steps, na.rm=TRUE)

# mean calc goes here - TBD!!!

```

```{r, echo=TRUE}
# Look at the average daily activity patterm
pattern <- aggregate(steps ~ interval, data=activity, FUN="sum")
plot(pattern$interval, pattern$steps, type="l")

# Question: which 5-min interval (snip) contains the max number of steps?

## this is the value when the MAX occurs, need the MAX interval -- TBD!!
max(pattern$steps, na.rm=TRUE)

```

```{r, echo=TRUE}
#Calculate the total number of missing values
missing_count <- nrow(subset(activity, is.na(steps)))
print(missing_count)
print ("this is the number of missing values")
print ("it is this percent of the total number of values:")
print(17568/missing_count)


# Inputting missing values -- fill N/A with mean value
#steps <- c()
#for (i in 1:nrow(activity)) {
#  if (is.na(activity$steps[i])) {
#    index <- which(pattern$interval == activity$interval[i])
#    steps[i] <- pattern$steps[index]
#  } else {
#    steps[i] <- activity$steps[i]
#  }
#}
#date <- activity$date
#interval <- activity$interval
#filled_data <- activity.frame(steps, date, interval)
#head(filled_data)


```


